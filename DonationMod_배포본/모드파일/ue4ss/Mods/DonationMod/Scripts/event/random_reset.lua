local function a(b)local c=DonationScriptDirectory.."\\event\\"..b..".lua"local d,e=loadfile(c)if d==nil then return nil,"이벤트 파일을 불러올 수 없습니다: "..tostring(e)end;local f,g=xpcall(d,debug.traceback)if not f or type(g)~="function"then return nil,"이벤트 파일 초기화에 실패했습니다: "..tostring(g)end;return g,nil end;return function(h)local i;if math.random(1,100)<=50 then i={eventName="delete_random_party_pal",message="[후원] 100,000원: 파티 펠 1마리를 무작위로 삭제합니다."}else i={eventName="clear_inventory",message="[후원] 100,000원: 일반 인벤토리 전체를 초기화합니다."}end;local j,e=a(i.eventName)if j==nil then return false,e end;local k,l,m=xpcall(function()return j(h)end,debug.traceback)if not k or not l then local n=k and m or l;h.log("100,000원 랜덤 초기화 실패: "..tostring(h.playerName).." / "..tostring(n))return false,tostring(n)end;h.sendSystemToPlayer(h.playerUid,i.message)h.log("100,000원 랜덤 초기화 예약: "..tostring(h.playerName).." / "..i.eventName)return true,i.message end